# AOW3 Community Wiki

Static portal prototype for the AOW3 community wiki concept.

Open `index.html` in a browser. Section pages are split into standalone HTML files, and content links are placeholders until real content and routing are added.
